# myvitals — Pending Work

Generated by session-close on 2026-05-26 after shipping **v0.7.274**.

Numeric task IDs are session-scoped. Use **mnemonics** (e.g. `FITBIT-2`)
as the durable identifier. At the start of a new session, ask Claude
to "rehydrate the task tracker from TODO.md + memory" and it'll
re-instantiate via TaskCreate with the right blockedBy edges.

---

## #SPO2 — wire SpO2 end-to-end (deferred, v0.7.292 review)

The phone declares `READ_OXYGEN_SATURATION` (manifest + `requiredPermissions`),
prompts for it, and counts it in the "12/12 granted" inventory — but
`SyncWorker.runSync` never reads `OxygenSaturationRecord`, `DataMapper.toBatch`
has no spo2 param, and `IngestBatch` has no spo2 field. So SpO2 never leaves the
phone even on a healthy device. Deferred this session because (a) it needs a full
chain — `safeRead` → `SpO2Sample` model → `IngestBatch.spo2` field → backend
ingest column + migration → query/UI — and (b) the PW3/PW4 firmware bug (see
CLAUDE.md) currently zeroes SpO2 anyway, so wiring it yields no data until Google
ships the sensor-permission fix. **When that firmware fix lands, do the full
wiring.** Until then the inventory count is mildly misleading.

---

## #FITBIT — Google Health migration (1 task remaining)

**Plan:** local Claude memory: `project_fitbit_google_health_migration.md`

Google rebranded the Fitbit Android app to "Google Health" on
**2026-05-19**. Calendar gate has passed. FITBIT-1 / FITBIT-3 /
FITBIT-4 / FITBIT-5 shipped in v0.7.271. Only the export-parser
verification remains — blocked on pulling a Google Health Takeout
ZIP. Likely JSON-only (legacy fitbit.com gave both CSV + JSON);
the parser may silently skip files if folder layout shifted.

| Mnemonic | Subject | Blocked by |
|---|---|---|
| FITBIT-2 | Test `parse_fitbit_zip` against a Google Health Takeout export | export ZIP from takeout.google.com |

When the ZIP is in hand: drop it on a dev backend, hit
`POST /imports/fitbit`, watch for `log.info("legacy fitbit wrist-temp
file parsed …")` (would indicate the new export still includes it,
contrary to Google's announcement) and confirm `files_seen` covers
heart rate / steps / sleep / HRV.

---

## Coach surface — future sub-cards (deferred)

The Coach view at `/coach` (web) + phone CoachScreen has six
cards (workout / cardio / sleep / recovery / fasting / goals).
Remaining ideas:

- **AI batch-3 spillover** — diff view ("this week vs last week"
  structured comparison), source citations (clickable evidence →
  data points), smart cache invalidation, local LLM (Ollama)
  provider, voice Q&A.

---

## Operational / housekeeping

- **Homelab CLAUDE.md outdated** — lists CT 104 as pinned to a
  single node, but ProxBalance has migrated it across several
  nodes recently. Switch to the pitstop-style "currently pveX,
  dynamic" pattern.
- **recovery_stale flag** — v0.7.268 removed the regenerate
  banner; v0.7.269 removed sleep from the planner. Backend still
  populates the `recovery_stale` field. Could be deleted outright
  in a future cleanup if no new use emerges.
- **`offBodyMarkArea` helper** in `frontend/src/components/charts/chartHelpers.ts`
  is unreferenced after v0.7.272. Tiny — left in place. Safe to
  remove if preferred.
- **Other interval-anchored APScheduler jobs** — v0.7.274 fixed
  Strava's restart-resets-interval landmine via `next_run_time` +
  self-throttle. The HA job uses `next_run_time=None` deliberately
  (don't fire on boot), but the weekly AI digest and any future
  long-interval jobs could trip the same trap. Audit if more long
  intervals get added.

---

## Other deferred

- **Multi-user** — single-user assumed throughout; no `user_id`
  column on most tables. Big refactor if ever wanted.
- **Strength v2 catalog supplement** — already shipped (69 entries
  in `data/exercises_supplement.json`); no work needed. Listed here
  to prevent re-filing.

---

## Already shipped recent (cross-reference, latest first)

- v0.7.274 — Strava sync fix (boot+30s first-run, 1h interval, throttle)
- v0.7.273 — Tap exercise icon → bigger image + how-to dialog (phone)
- v0.7.272 — Drop off-wrist mark-area from HR charts (web + phone)
- v0.7.271 — FITBIT-3 + FITBIT-4 + Google Health rebrand defenses
- v0.7.270 — Missed-strength carry-over to today's slot
- v0.7.269 — Remove sleep from workout planner decisions
- v0.7.268 — Hide recovery_stale regenerate banner
- v0.7.267 — DAYNAV-2: Sleep day picker + HR `bucket_seconds` + Steps past-day bug
- v0.7.266 — DAYNAV-1: Day selector on Steps / HR / VitalsDetail
- v0.7.265 — Phone Material3 TimePicker for "Ended at"
- v0.7.264 — CARDIO-LOG-2: Edit past manual cardio entries
- v0.7.263 — Cardio-log dialog adjustable "Ended at" so HR window aligns
- v0.7.262 — `complete-cardio` HR query fix (column is `bpm`)
- v0.7.261 — CARDIO-LOG-1: Manual cardio logging on cardio-day complete
- v0.7.260 — OFFLINE-3 + OFFLINE-4: SWR cache on remaining phone screens
- v0.7.259 — OFFLINE-2: SWR cache on Strength family
- v0.7.258 — OFFLINE-1: SyncWorker auto-flush + workout cold-start offline message
- v0.7.257 — Sleep tile/detail agree (asleep time)
- v0.7.256 — Cache vitals profile to eliminate first-frame tile flicker

Durable design docs:
- FAST-COACH: `docs/FAST_COACH_PLAN.md`
- FITBIT migration: local Claude memory: `project_fitbit_google_health_migration.md`
