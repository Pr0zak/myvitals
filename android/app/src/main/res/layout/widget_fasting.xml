<?xml version="1.0" encoding="utf-8"?>
<!--
  2×1 fasting widget. RemoteViews-compatible (no custom view). Layout
  is intentionally simple so it survives across launcher themes.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:orientation="vertical"
    android:padding="12dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/widget_elapsed"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:textColor="#E6EAF2"
            android:textSize="20sp"
            android:textStyle="bold"
            android:text="—h —m"
            android:fontFamily="sans-serif" />

        <TextView
            android:id="@+id/widget_pct"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#94A3B8"
            android:textSize="11sp"
            android:text="0%" />
    </LinearLayout>

    <TextView
        android:id="@+id/widget_stage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="#EF4444"
        android:textSize="10sp"
        android:letterSpacing="0.08"
        android:textStyle="bold"
        android:text="NO ACTIVE FAST"
        android:paddingTop="2dp" />

    <ProgressBar
        android:id="@+id/widget_progress"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="6dp"
        android:layout_marginTop="6dp"
        android:max="100"
        android:progress="0"
        android:progressDrawable="@drawable/widget_progress" />
</LinearLayout>
