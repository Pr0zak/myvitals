# myvitals — Pending Work

Generated by session-close on 2026-05-15 after shipping **v0.7.206**.

Numeric task IDs are session-scoped. Use **mnemonics** (e.g. `FITBIT-1`)
as the durable identifier. At the start of a new session, ask Claude
to "rehydrate the task tracker from TODO.md + memory" and it'll
re-instantiate via TaskCreate with the right blockedBy edges.

---

## #FITBIT — Google Health migration (5 tasks · calendar-gated)

**Plan:** `~/.claude/projects/-home-spider-health-app/memory/project_fitbit_google_health_migration.md`

Google rebrands the Fitbit Android app to "Google Health" on
**2026-05-19** (auto-update rollout May 19–26). Three tasks are blocked
on the rebrand actually landing on the user's phone; two others need
a Google Health export ZIP to validate the parser. Do **not** start
before 2026-05-19.

| Mnemonic | Subject | Blocked by |
|---|---|---|
| FITBIT-1 | Post-May-19 sync smoke test (Pixel Watch 3 → Google Health → HC) | calendar |
| FITBIT-2 | Test `parse_fitbit_zip` against a Google Health export | calendar |
| FITBIT-3 | Update Settings.vue + README copy for Google Health rebrand | FITBIT-2 |
| FITBIT-4 | Mark `_parse_fitbit_wrist_temp` as legacy | FITBIT-2 |
| FITBIT-5 | HC background-read permission parity check under Google Health | calendar |

**Recommended order:** FITBIT-1 + FITBIT-5 in parallel → FITBIT-2 →
FITBIT-3 + FITBIT-4 in parallel.

---

## #ANALYTICS — Unified analytics surface (3 tasks remaining)

ANALYTICS-1 (web /analytics page with 4 tabbed lenses) shipped in
v0.7.203 along with NAV-1 (SideNav collapse). The remaining tasks are
per-view refactors of Trends/Insights/Compare/Coach to participate in
shared range state, and a Coach payload upgrade.

| Mnemonic | Subject | Blocked by |
|---|---|---|
| ANALYTICS-2 | Shared time-range selector across all 4 Analytics tabs | — |
| ANALYTICS-3 | Cross-tab drill-down — jump between lenses with context preserved | ANALYTICS-1 (done) |
| ANALYTICS-4 | Coach payloads gain `top_correlations` + `wow_deltas` | — |

ANALYTICS-2 is moderately involved because the four lenses have
heterogeneous range types ("7d"|"30d"|"90d"|"365d" vs numeric `days`
ref vs no range at all on Coach). Each lens needs a small refactor
to read its range from the URL query param rather than its own ref.

---

## #NAV — Navigation consolidation (DONE)

| Mnemonic | Subject | Blocked by |
|---|---|---|
| ~~NAV-1~~ | ~~Web SideNav collapse~~ | **shipped v0.7.203** |
| ~~NAV-2~~ | ~~Phone chip vs action distinction~~ | **shipped v0.7.199** |

---

## #GOALS — Goal surface integration (5 tasks remaining)

GOALS-1 (bidirectional profile↔goal sync) shipped in v0.7.198 and
unblocked the rest of the family. The remaining work surfaces goals
across the existing charts and chips.

| Mnemonic | Subject | Blocked by |
|---|---|---|
| GOALS-2 | Goal lines + trajectory cone on detail charts | — |
| GOALS-3 | Progress on Vital chips + Today Goals tile | GOALS-2 |
| GOALS-4 | Phone parity — GoalsScreen + Vital chip | GOALS-3 |
| GOALS-5 | "Goals coach" card on the Coach surface | — |
| GOALS-6 | Auto-complete + celebrate when a goal target is crossed | — |

---

## #TODAY — Today page modernisation (4 tasks remaining)

| Mnemonic | Subject | Blocked by |
|---|---|---|
| TODAY-1 | Add Sober + Fasting + Goals + Coach tiles to Today | ANALYTICS-1 |
| TODAY-2 | Drop AIInsights from Today; keep Hero verdict; link to Coach tab | ANALYTICS-1 |
| TODAY-3 | Tap-through on every Today tile to its detail view | TODAY-1 |
| TODAY-4 | Bundle Today's 13 parallel calls into `/summary/today/snapshot` | TODAY-1 |
| ~~TODAY-5~~ | ~~AnnotationLog dead-or-render~~ | **shipped v0.7.200 (rendered)** |

---

## #LOG — Annotation surface (DONE for now)

The whole #LOG family shipped this session:
- ~~LOG-1~~ — `/log` → `/journal` rename + 308 shims (v0.7.198)
- ~~LOG-2~~ — Today quick-add buttons (v0.7.200)
- ~~LOG-3~~ — phone JournalScreen with quick-add row (v0.7.205)
- ~~LOG-4~~ — emoji annotations on the HR 24h chart, web (v0.7.206)

**Phone-parity follow-up for LOG-4:** the phone HR chart is a Compose
Canvas drawing, not ECharts. Equivalent emoji markpoints need a
separate `drawText` pass over the annotation timestamps in
HrDetailScreen.kt's `LiveHrChart`. Left as a small follow-up rather
than blocking the LOG-family close.

---

## #WATCH — Wear-state surfacing (DONE)

| Mnemonic | Subject | Blocked by |
|---|---|---|
| ~~WATCH-1~~ | ~~Charts on /watch~~ | **shipped v0.7.202** |
| ~~WATCH-2~~ | ~~Off-wrist overlay on HR charts~~ | **shipped v0.7.204** (web + phone) |

---

## #SETTINGS — Settings layout (1 task)

| Mnemonic | Subject | Blocked by |
|---|---|---|
| SETTINGS-1 | Settings.vue — internal tab layout instead of accordion | — |

---

## #VITALS — Detail-view coverage (1 task)

| Mnemonic | Subject | Blocked by |
|---|---|---|
| VITALS-1 | Add /steps /hrv /skin-temp standalone vital detail views | — |

---

## AI integration — batch 3 (deferred)

### Phone-side alert notifications

Backend produces structured alerts (`/ai/alerts`); since v0.7.128
these are generated lazily on read (when the most recent alert is
>6h old). ALERTS-1 retired the standalone web /alerts page, ALERTS-2
(v0.7.201) feeds historical alerts into Coach payloads. The companion
app still needs to surface them as system notifications.

- [ ] Add `aiAlerts()` + `markAlertsNotified()` to `BackendClient.kt`
- [ ] Inside `SyncWorker.doWork()`, after a successful sync, fetch
      `GET /ai/alerts?unacked_only=true`. For any row whose
      `phone_notified_at` is null, post a `Notifier.postAiAlert(...)`
      and `POST /ai/alerts/mark-notified` with the IDs to suppress
      duplicates.
- [ ] Extend `Notifier` with a second channel `alerts` (severity-based
      icons, distinct from the `updates` channel).
- [ ] Tapping the notification deep-links into the dashboard via
      `https://<backend>/?alert=<id>` — frontend route handler scrolls
      to the relevant card.
- [ ] Optional: a small "AI alerts" toggle in phone Settings →
      Diagnostics so users can mute the channel without touching
      Health Connect grants.

### Other AI deferrals (lower priority)

- [ ] **Diff view** on Today — "this week vs last week" side-by-side
      structured cards. Backend `POST /ai/explain/diff?ranges=…`
      returns two structured outputs and a delta narrative.
- [ ] **Source citations** — make claims in AI cards clickable to
      jump to the underlying data point ("HRV 11ms on May 6" → opens
      `/trends#hrv` zoomed to that day). Needs a structured response
      schema with `(text, link)` tuples.
- [ ] **Smart cache invalidation** — currently any tiny payload-hash
      delta re-bills. Could whitelist only material fields (changing
      `notes` shouldn't bust the sleep cache).
- [ ] **Local LLM option** — Ollama / Llama backend; same `/ai/*`
      endpoints, different SDK behind a config flag. Pure privacy
      play.
- [ ] **Voice Q&A** on the phone — TTS the answer; tap-to-speak
      input via Android speech recognizer.
- [ ] **HA env enrichment** — `env_readings` (bedroom temp/humidity)
      already in DB but not in Claude's payload. Adding it lets the
      model correlate sleep with environmental factors.

---

## Coach surface — future sub-cards

The Coach view at `/coach` (web) + Workout → ⋮ → Coach (phone). Two
cards shipped v0.7.190–v0.7.191 (Workout coach, Cardio coach), and the
payloads now see historical alerts (ALERTS-2, v0.7.201). Natural
follow-ups:

- [ ] **Sleep coach** — synthesizes sleep duration, consistency,
      stage breakdown, sleep_debt_h, and recent HRV/RHR drift into a
      "sleep is / isn't supporting recovery" verdict.
- [ ] **Recovery coach** — broader than the existing deload-check.
      Includes overnight HRV / RHR / skin temp delta / readiness with
      multi-week trend framing.
- [ ] **In-process re-arm** — `_ha_run()` task currently only
      re-reads `ha_config` on each disconnect. Settings toggle today
      needs a backend restart. Add a SIGUSR1-or-similar nudge so
      toggling realtime live doesn't require restart.

---

## HA realtime sync — future

The trimmed scope (#HA-1..8) shipped in v0.7.182 / v0.7.189. Remaining
ideas if HA + HC ever both carry overlapping HR / steps:

- [ ] **Source-priority dedupe** in `analytics/jobs.py::compute_daily_summary`.
      Originally tracked as #HA-9; deleted because the trimmed scope
      doesn't have HA carrying HR or steps. Refile if the Wear OS
      Companion App ever starts emitting per-second HR streams.

---

## Other deferred features

- [ ] **Strength v2 — catalog supplement** — add ~25 named exercises
      Fitbod has that free-exercise-db doesn't (Incline Row, Kroc Row,
      Bird Dog Row, Renegade Row, Tate Press, Single-Arm Floor Press,
      B-Stance RDL, DB Hip Thrust, Cuban Rotation, Zottman Preacher,
      Cross-Body Hammer, Plank Pull-Through, Pike / Archer / Hindu
      push-ups, etc.) as a supplementary JSON merged at catalog load.
- [ ] **iOS app** — currently Android-only. Same Health Connect
      analogue would be HealthKit on iOS.
- [ ] **Multi-user** — single-user assumed throughout; no user_id
      column on most tables. Big refactor if ever wanted.
- [ ] **Watch-side complication** — Wear OS face complication
      showing sober days / readiness / verdict.

### Already shipped (kept for cross-reference)

- v0.7.0 — Trail-status integration
- v0.7.2 — Trail-status phone surface
- v0.7.180 — CI Node 20 → 24 migration (INFRA-1)
- v0.7.179 — Phone equipment editor (PHONE-EQUIP-1)
- v0.7.182 / v0.7.189 — HA realtime device_status (HA-1..8)
- v0.7.183 → v0.7.192 — Intermittent fasting (FAST-1..15)
- v0.7.190 / v0.7.191 — AI Coach surface (COACH-1..4)
- v0.7.198 — ALERTS-1 (drop /alerts), LOG-1 (rename /log→/journal),
  GOALS-1 (bidirectional profile↔goal sync)
- v0.7.199 — NAV-2 (phone chip visual distinction)
- v0.7.200 — LOG-2 + TODAY-5 (Today AnnotationLog rendered + quick-add)
- v0.7.201 — ALERTS-2 (recent_alerts in Coach payloads)
- v0.7.202 — WATCH-1 (battery / on-body / activity charts on /watch)
- v0.7.203 — ANALYTICS-1 + NAV-1 (unified /analytics page + SideNav collapse)
- v0.7.204 — WATCH-2 (off-wrist overlay on 24h HR chart, web + phone)
- v0.7.205 — LOG-3 (phone JournalScreen + quick-add row)
- v0.7.206 — LOG-4 (annotation emoji markpoints on HR chart, web)
