# myvitals — Pending Work

Generated by session-close on 2026-05-16 after shipping **v0.7.252**.

Numeric task IDs are session-scoped. Use **mnemonics** (e.g. `FITBIT-1`)
as the durable identifier. At the start of a new session, ask Claude
to "rehydrate the task tracker from TODO.md + memory" and it'll
re-instantiate via TaskCreate with the right blockedBy edges.

---

## #FITBIT — Google Health migration (5 tasks · calendar-gated)

**Plan:** `~/.claude/projects/-home-spider-health-app/memory/project_fitbit_google_health_migration.md`

Google rebrands the Fitbit Android app to "Google Health" on
**2026-05-19** (auto-update rollout May 19-26). Three tasks are
blocked on the rebrand landing on the user's phone; two more need a
Google Health export ZIP to validate the parser. Do **not** start
before 2026-05-19.

| Mnemonic | Subject | Blocked by |
|---|---|---|
| FITBIT-1 | Post-May-19 sync smoke test (Pixel Watch 3 → Google Health → HC) | calendar |
| FITBIT-2 | Test `parse_fitbit_zip` against a Google Health export | calendar |
| FITBIT-3 | Update Settings.vue + README copy for Google Health rebrand | FITBIT-2 |
| FITBIT-4 | Mark `_parse_fitbit_wrist_temp` as legacy | FITBIT-2 |
| FITBIT-5 | HC background-read permission parity check under Google Health | calendar |

**Recommended order:** FITBIT-1 + FITBIT-5 in parallel → FITBIT-2 →
FITBIT-3 + FITBIT-4 in parallel.

---

## Mechanical follow-ups (no mnemonic — pattern is established)

### Phone SWR cache + pull-to-refresh on remaining detail screens

v0.7.252 set the pattern on Steps / HR / Sleep detail. CLAUDE.md
"Phone SWR + pull-to-refresh" section has the recipe. Apply to:

- HrvDetailScreen.kt
- SkinTempDetailScreen.kt (it's in VitalsDetailScreen.kt today)
- WeightDetailScreen.kt
- BpDetailScreen.kt
- ActivitiesScreen.kt
- CoachScreen.kt (preload-latest pattern needs an SWR wrapper too)

Each is ~30 lines of edits: extract `LaunchedEffect` body into
`suspend fun fetch()`, add `JsonCache.write` after each fetch, new
`LaunchedEffect` reads cache then fetches, wrap LazyColumn in
`PullableMetricBox`.

---

## Coach surface — future sub-cards (mostly deferred)

The Coach view at `/coach` (web) + phone CoachScreen now has six
cards (workout / cardio / sleep / recovery / fasting / goals).
Remaining ideas:

- **AI batch-3 spillover** — diff view ("this week vs last week"
  structured comparison), source citations (clickable evidence →
  data points), smart cache invalidation, local LLM (Ollama)
  provider, voice Q&A.

---

## Other deferred

- **Multi-user** — single-user assumed throughout; no `user_id`
  column on most tables. Big refactor if ever wanted.
- **Strength v2 catalog supplement** — already shipped (69 entries
  in `data/exercises_supplement.json`); no work needed. Listed here
  to prevent re-filing.

---

## Already shipped this batch (cross-reference, latest first)

- v0.7.252 — Phone SWR + pull-to-refresh on Steps / HR / Sleep detail
- v0.7.251 — "Today by hour" steps chart (web + phone)
- v0.7.250 — Steps cross-source over-count fix (`pick_canonical_steps_source`)
- v0.7.249 — `/update` auth relaxed to `require_any` (phone 401 fix)
- v0.7.248 — FAST-18 fasting-aware strength generation + UI banner
- v0.7.247 — FAST-16 "Should I fast today?" coach card
- v0.7.246 — FAST-17 `fast_streak` AiGoal kind + migration 0037
- v0.7.245 — FAST-19 `fasting_status` plumbed into coach payloads
- v0.7.244 — APK updater inline progress (kills full-screen activity)
- v0.7.243 — STRENGTH-3 Moshi default fix
- v0.7.242 — AI-1 HA env_readings in sleep + recovery coach payloads
- v0.7.241 — COACH-6 Recovery coach card
- v0.7.240 — HA-9 live re-arm + COACH-5 Sleep coach card

Durable design doc for FAST-COACH: `docs/FAST_COACH_PLAN.md`.
